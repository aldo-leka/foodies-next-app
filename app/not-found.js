export default function NotFound() {
    return (
        <main className="not-found">
            <h1>Not found</h1>
            <p>Unfortunately we couldn&apos;t find the resource.</p>
        </main>
    );
}